version: "3"
services:
    ns3:
        build: 
            context: ./ns3_worker
        tty: true
        privileged: true
        volumes:
            - /lib/modules:/lib/modules
        depends_on:
            - maci-backend
        restart: always
        hostname: ns3
